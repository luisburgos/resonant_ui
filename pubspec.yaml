name: resonant_ui
description: To evoke a fealing of shared emotion or belief with someone.
version: 0.0.1
homepage: https://github.com/luisburgos/resonant_ui

environment:
  sdk: '>=2.14.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Remove FontAwesome dependency when custom Icons are added to the project.
  font_awesome_flutter: 9.2.0
  mr_skeleton: 0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  lints: ^1.0.0
  test: ^1.16.0
