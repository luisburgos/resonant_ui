name: resonant_ui
description: To evoke a fealing of shared emotion or belief with someone.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: '>=2.14.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Remove FontAwesome dependency when custom Icons are added to the project.
  font_awesome_flutter: 9.2.0
  mr_skeleton:
    git:
      url: git@github.com:LuisBurgos/mr_skeleton.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  lints: ^1.0.0
  test: ^1.16.0
